<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phoenixproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoenixProject">
        <activity android:name=".ui.main.Home"></activity>
        <activity android:name=".ui.ScanActivity" />
        <activity android:name=".ui.main.suppliers.SupplierDetailsActivity" />
        <activity android:name=".ui.main.products.ProductDetailes" />
        <activity android:name=".ui.main.products.ProductsActivity" />
        <activity android:name=".ui.main.invoice.Invoices_Activity" />
        <activity android:name=".ui.main.Home" />
        <activity android:name=".ui.main.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>